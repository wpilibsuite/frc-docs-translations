version: 2

sphinx:
  builder: html
  configuration: conf.py
  fail_on_warning: false

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  apt_packages:
    - librsvg2-bin
  python:
   install:
      - method: uv
        command: sync

formats:
  - htmlzip
  - pdf

submodules:
  include: all
